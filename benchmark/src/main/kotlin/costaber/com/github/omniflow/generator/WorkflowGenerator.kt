package costaber.com.github.omniflow.generator

import costaber.com.github.omniflow.model.Workflow

class WorkflowGenerator {

    fun generate(number: Int): Collection<Workflow> {
        return emptyList()
    }

}